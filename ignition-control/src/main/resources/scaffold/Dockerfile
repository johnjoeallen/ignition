# Starter — replace with your app. Keep EXPOSE'd port matching the app's
# APP_PORT repo variable (Settings -> Actions -> Variables) so deploys route
# correctly.
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html
EXPOSE 80
